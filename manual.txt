docker compose up -d --build
docker exec -it converter-php bash -c "mkdir -p backend && cd backend && composer require aws/aws-sdk-php"
docker exec -it converter-php bash -c "cd backend && composer require setasign/fpdf"