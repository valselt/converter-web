docker compose up -d --build
docker exec -it converter-php bash -c "cd backend && composer require aws/aws-sdk-php"
docker exec -it converter-php bash -c "cd backend && composer require setasign/fpdf"
docker exec -it converter-php chown -R www-data:www-data /var/www/html
docker exec -it converter-php chmod -R 775 /var/www/html/backend


untuk casaos-cnvrt :
chmod +x php/entrypoint.sh
docker build -t local/cnvrt-php:latest php

LALU DROP .yaml ke CASAOS Customized App